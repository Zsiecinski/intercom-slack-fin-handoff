{
  "name": "intercom-slack-fin-handoff",
  "version": "1.0.0",
  "type": "module",
  "description": "Webhook service to notify Slack when Intercom conversations with Fin are assigned to admins",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test-tokens": "node src/test-tokens.js",
    "test": "node src/test-tokens.js"
  },
  "keywords": [
    "intercom",
    "slack",
    "webhook"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.0"
  }
}

